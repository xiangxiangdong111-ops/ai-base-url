{
  "name": "ai-base-url",
  "version": "0.1.0",
  "private": true,
  "description": "A verified registry and static web UI for OpenAI-compatible and Anthropic-compatible API base URLs.",
  "type": "module",
  "scripts": {
    "validate": "node scripts/validate.js",
    "generate": "node scripts/sync-docs-data.js",
    "generate:check": "node scripts/sync-docs-data.js --check",
    "check": "node scripts/check.js",
    "serve": "node scripts/serve-docs.js"
  },
  "keywords": [
    "ai",
    "llm",
    "base-url",
    "openai-compatible",
    "anthropic-compatible"
  ],
  "license": "MIT"
}
